# Document-Agent

基于多 Agent 协作的桌面 AI 助手，支持文件夹分析、代码审查、文档摘要、文件整理、**智能表单填写**等功能。

> **当前版本：v3.0.0**

## 版本历史

### 3.0.0（2026-08-23）

#### 多模型支持扩展

- **新增小米 MiMo 模型**：支持 MiMo API Key 配置，兼容 MiMo OpenAI 格式 API（`api.xiaomimimo.com`），默认模型 `mimo-v2.5-pro`
- **Provider 列表扩展**：设置面板新增「小米 MIMO」API Key 输入行（橙色标识），与其他厂商样式统一

#### 侧边栏 UI 优化

- **三区块折叠统一**：文件夹、Agent、历史三个区块的折叠/展开行为完全统一，折叠后仅显示标题行
- **UI 界面优化**：侧边栏横向滚动禁用、右边缘缝隙消除、滚动条样式优化

#### Agent 团队重构（核心）

- **11 Agent 并行协作体系**：Oliver（调度）/ Charlotte（文件分析）/ Amelia（文档摘要）/ Ethan（信息采集与表单填写）/ Atlas（架构设计）/ Audrey（深度研究）/ Avery（QA 与代码审查）/ Aurora（日常事务）/ Aria（内容生成）/ Arthur（文档与演示）/ Alice（浏览器控制）
- **Leader 路由引擎增强**：确定性关键词预检 + 连续对话路由 + 表单填写强制拦截
- **Agent 对话面板**：右侧面板实时展示子 Agent 工作过程，支持新建对话一键清空

#### 平台抽象层（PlatformAPI）

- 引入 `PlatformAPI` 抽象接口，所有 Agent 通过统一接口调用原生能力（文件系统 / 存储 / 窗口 / OS），为跨平台（鸿蒙等）预留扩展
- **新增能力**：行级 diff（LCS 算法）、文件监听（mtime 轮询）、系统桌面通知

#### 智能记忆系统

- **L0-L3 记忆层级**：瞬时上下文 → 会话历史 → 会话总结 → 长期偏好/规则，含 levelTTL 自动过期
- **统一记忆存储**：基于 key-value 的持久化层，按层级索引

#### 定时任务引擎

- cron 5 字段解析/匹配引擎，全局调度器，支持定时触发 Agent 任务

#### 能力组件库（9 个能力面板）

- **文件管理器**（FileManager）：创建/移动/重命名/删除/下载/预览
- **代码编辑器**（CodeEditor）：高亮/行号/保存/复制
- **Diff 对比器**（DiffViewer）：行级 diff 可视化
- **图表设计器**（DiagramDesigner）：支持 27 种 mermaid 图表类型（流程图/架构图/时序图等）
- **任务调度器**（TaskScheduler）：定时任务 CRUD
- **插件管理器**（PluginManager）：本地工具探测与状态
- **研究报告器**（ResearchReport）：深度研究报告结构输出
- **文档生成器**（DocumentProduction）：Word/PPT/Excel/PDF 生成
- **能力中心**（CapabilitiesHub）：9 个能力组件统一入口

#### 文档与演示生产力

- **Word/PPT/Excel/PDF 生成**：基于 `docx` / `pptxgenjs` / `xlsx` / `pdf-lib`
- **PDF 智能分析**：pdf-inspector CLI 探测 + pdf-parse 回退
- **代码审查 CLI**：open-code-review CLI 桥接 + 本地规则回退

#### Skill 系统

- **diagram-design Skill**：27 种视觉类型 + Brutalist 风格指南
- **code-review Skill**：按语言/路径/变更类型的审查规则
- **skills 路由配置**：41 条 Agent 路由规则（routing.json）
- **竞品分析模板**（competitive.md）：Audrey 交叉验证方法论与矛盾标注

#### 代码基础设施

- **代码调用关系分析**（codeGraph）：import/调用边分析 + 反向引用/统计
- **工具索引**（toolRegistry）：扫描 15 个 CLI 工具可用性与版本

#### 已知问题（后续更新逐步修复）

- Ethan Agent 部分文档填写场景仍存在下划线错位、填写失败等历史问题
- 部分能力组件（浏览器控制、代码审查 CLI 完整集成）依赖外部环境，暂为框架态

---

### 2.1.5（2026-07-30）

#### 会话路由与状态管理修复

- **修复退出表单填写后消息错误路由**：退出表单填写会话时 `activeAgentId` 未重置为 null，导致后续普通对话被错误路由到 Ethan（表单填写 Agent）
- **修复子 Agent 对话气泡为空**：MemoryAgent 等子 Agent 的 `execute()` 直接 return 字符串而未调用 `onToken` 回调，导致 ChatInput 创建的消息气泡内容始终为空；新增兜底逻辑在 `execute()` 返回后将结果写入气泡
- **新增 `updateMessageByIndex` 方法**：支持按索引更新任意消息内容，替代原来只能更新最后一条消息的限制
- **退出提示文案修正**：退出表单填写时，将过时的"请在下方勾选需要填写的字段"提示正确替换为"已手动退出，项目终止"

#### 闲聊意图识别重构

- **LLM 意图判断 + 快速路径兜底**：将闲聊/问候判断从纯硬编码正则改为三层架构——快速路径（确定性规则）→ 任务关键词排除 → LLM 兜底判断
- **扩大快速路径覆盖**：新增口语化思考（"我想想"）、疑惑困惑（"什么意思"）、纯标点（"？？？"）、极短消息等模式匹配
- **按语境分类回复**：`answerCasual` 区分问候、疑惑、思考、纯标点等场景，给出贴合语境的自然回复，避免"好的，有需要随时叫我！"的机械回复

#### UI 优化

- **加载动画升级**：消息气泡加载状态从闪烁竖线光标改为 brutalist 风格"正在思考中"三点弹跳动画，严格遵循现有设计风格

### 2.1.4（2026-07-29）

#### 文本选择修复

- **修复对话消息无法复制**：在 `globals.css` 的 `.msg-agent` 和 `.msg-user` 类上添加 `user-select: text`，解决 `body` 全局 `user-select: none` 导致 Agent 生成内容无法选中和复制的问题
- **修复 FormFillView 文本不可复制**：在信息采集助手的 Agent 消息容器上补上 `msg-agent` 类，确保表单填写流程中的对话气泡也能正常复制文本

### 2.1.3（2026-07-28）

#### 文档分析进度条

- **新增分析进度条组件**：在 Ethan 分析文档时，消息气泡中实时显示 6 步分析进度（读取文件 → 提取文本 → 分析结构 → 构建提示词 → AI 提取 → 交叉验证）
- **线性推进动画**：每步最小显示 400ms，进度条平滑过渡，避免瞬间跳变
- **流光效果**：进行中步骤有 shimmer 流光动画，视觉反馈清晰
- **完成后淡出**：全部完成后停留 1.5 秒，然后平滑淡出消失
- **Brutalist 风格**：严格遵循现有 brutalist 设计风格（粗黑边框、硬阴影、黄色主题色）

### 2.1.2（2026-07-27）

#### 防错位机制增强 & UI 修复

- **列索引强锁定方案**：明确表格第0列为只读题干列、第1列为可填写列，通过 `fillTableRightCellByTopic` 函数仅匹配左栏题干并往同行右栏填写，杜绝串格
- **单元格读写白名单方案**：遍历表格时自动标注列角色，题干说明列开启只读锁定，写入时直接跳过锁定单元格
- **下划线填写优化方案**：锁定固定前缀不动，仅处理段落末尾带下划线格式的占位片段，避免固定文字被误改
- **通用前置校验规则**：新增区域角色划分、占位符真伪判定（仅下划线/【】/书签/内容控件为可修改占位）、上下文绑定定位（左栏题干→同行右栏成对关系）
- **进度显示修复**：将"写入文件"进度独立追踪，避免填写成功但写入失败时进度显示不一致
- **重试按钮**：失败后显示重试按钮，支持"重试写入文件"和"重新填写"两种模式

### 2.1.1（2026-07-27）

#### Ethan Agent 技术路线重构

**防错位机制全面升级：**

- **段落 Run 合并替换法**：解决 Word 文档中占位符被拆成多个 Run 导致的下划线错位问题。遍历段落所有 Run 拼接完整文本，定位占位符后合并替换，全程继承原有格式
- **原生锚点定位法（书签/窗体控件）**：作为优先策略自动执行，100% 继承周边字体、下划线、对齐格式，完全杜绝错位
- **表格关键词锚定法**：不按行列号硬编码定位，通过关键词匹配目标单元格，免疫合并单元格、嵌套表格导致的索引错位
- **合并单元格兼容处理**：写入前判断单元格是否为合并单元格，仅向首单元格写入，避免内容不可见
- **固定列宽保护**：防止长文本撑宽单元格导致整表列宽错位
- **文档结构校验**：填写前后对比段落数、表格数、单元格数，结构异常时自动告警

**填写流程优化：**

- 新增书签/窗体控件优先策略，自动检测并优先使用原生锚点填写
- 智能填写引擎（原"正则匹配"）集成全部防错位机制，作为兜底全能方案
- 多方案降级流程增加可视化动态进度指示器

#### 已知问题

> 实话实说，Ethan Agent 目前仍存在不少 Bug，部分问题受限于技术架构短期内无法彻底修复。我们正在持续努力改进，感谢大家的耐心与期待。

- **下划线错位**：部分文档中占位符的 Run 碎片化程度超出预期，Run 合并替换法可能无法覆盖所有场景
- **填写内容未写入**：某些特殊格式的文档（如复杂嵌套表格、受保护文档）仍可能出现填写失败
- **表格填写错位**：合并单元格 + 嵌套表格的极端场景下，关键词锚定可能定位到错误单元格
- **LLM 字段提取不稳定**：面对复杂文档，LLM 可能遗漏字段或提取到错误的位置信息
- **书签/控件兼容性**：仅支持标准 Word 书签和窗体控件，部分第三方工具生成的文档可能不兼容

---

### 2.1.0（2026-07-12）

#### 侧边栏功能升级

- **新增 Agent 列表区块**：侧边栏新增"AI Agent"分区，展示全部可用 Agent（含名称、图标、描述），点击即可切换当前活跃 Agent，当前选中 Agent 高亮显示，Leader Agent 标注"推荐"徽章
- **新增对话历史区块**：侧边栏新增"历史"分区，展示所有已保存的对话会话，点击恢复历史对话，hover 显示删除按钮
- **三区块独立折叠**：文件夹、Agent、历史三个区块各自拥有独立的折叠/展开按钮，折叠后显示紧凑摘要行（彩色圆点 + 数量），释放侧边栏空间
- **Raft 风格按钮交互动效**：所有折叠按钮和底部操作按钮加入三段式动画——hover 浮起 + 阴影扩大，active 按下消失，过渡统一 150ms ease-out
- **半透明分割线**：列表条目之间使用半透明分割线，替代之前无分割的密集排列
- **微圆角与过渡统一**：彩色方块、图标外框、徽章加入细微圆角，全部过渡时长统一为 150ms

#### 表单填写交互简化

- **移除 LLM 意图分类**：表单填写会话中不再通过 LLM 判断用户意图（"跳过"/"AI 生成"/"下一个"等），所有文本输入直接作为当前字段的填写内容，减少 LLM 误判和延迟
- **按钮化操作**：跳过、AI 生成、下一个、完成等操作统一通过按钮触发，替代自然语言命令，交互更加明确可靠
- **提示文案更新**：填写引导语从命令式（"说'跳过'"）改为按钮式（"点击'跳过'按钮"）

#### Bug 修复

- **Agent 列表刷新后不加载**：Agent 数据从非响应式 Proxy 迁移到 Zustand store（新增 `sidebarStore.ts`），异步初始化完成后自动触发组件重渲染，不再需要手动折叠展开
- **历史会话重复保存**：新增 `currentSessionId` 追踪机制，切换会话时只保存新建的未保存对话，从历史加载的会话不再重复存储
- **历史会话缺少选中高亮**：当前选中的历史会话在侧边栏中显示黄色背景 + 内阴影，与 Agent/文件夹选中态统一

#### 其他

- 文件夹折叠状态从组件内部状态迁移至 `folderStore`，刷新后保持折叠状态

---

### 2.0.0（2026-07-10）

#### 核心新功能：Ethan 表单填写 Agent

**Ethan** 是 2.0.0 的核心功能，一个专门用于信息采集与文档填写的智能 Agent。

**工作流程：**
1. 用户发送文档填写请求（如"帮我填写附件一"）
2. Leader Agent 自动识别并路由给 Ethan
3. Ethan 解析文档，提取所有待填字段
4. 用户在对话中逐字段填写（支持手动输入或 AI 自动生成）
5. 填写完成后自动生成新文档并保存

**支持的文档格式：**
- Word 文档：`.docx`
- Excel 表格：`.xlsx`、`.xls`
- PDF 文档：`.pdf`
- 文本文件：`.txt`、`.md`、`.rtf`、`.csv`
- 标记语言：`.html`、`.htm`、`.json`、`.yaml`、`.yml`、`.xml`

**特色功能：**
- **字段选择界面**：用户可勾选需要填写的字段，跳过不需要的
- **文件选择界面**：文件夹中有多个文档时，让用户选择目标文档
- **AI 自动生成**：对每个字段，用户可选择让 AI 根据上下文自动生成内容
- **自然语言理解**：用户可以用自然语言交互（"下一个"、"跳过"、"帮我写"、"完成"等）
- **会话锁定**：填写过程中所有消息直接路由给 Ethan，不会被其他 Agent 干扰

#### Agent 系统增强

- **Leader Agent（Oliver）路由系统重构**：
  - 新增确定性关键词预检机制，避免 LLM 误判
  - 表单填写任务强制拦截，优先于多 Agent 协作检测
  - 支持模糊表达识别（"帮我填写文档"、"完成附件"、"弄一下这个表"等）
  - 连续对话路由：用户回复时自动路由回之前的 Agent
- **Agent 对话面板**：右侧面板实时展示子 Agent 的工作过程
- **新建对话功能**：顶部新增新建对话按钮，一键清空当前对话

#### 文档处理系统

- **统一文档解析入口**（docParser.ts）：支持 14 种文档格式的文本提取
- **Word 文档处理器**（docxHandler.ts）：基于 PizZip 直接操作 XML 结构，保留原始格式
- **Excel 文档处理器**（xlsxHandler.ts）：基于 PizZip 直接操作 xlsx 内部 XML 结构

#### 用户体验优化

- **Token 用量统计**：追踪每次 LLM 调用的 token 消耗
- **跨会话记忆系统**：记住重要信息，支持回忆历史分析记录
- **文件匹配逻辑增强**：递归子文件夹、中文数字匹配、单文档自动选择

#### Bug 修复

- 修复 clearAgentMessage 错误定位问题
- 修复 pdf-parse v2 导入导致的构建失败
- 修复表单填写会话中组件不消失的问题
- 修复保存成功后可以反复保存的问题

#### 已知问题（后续更新将会逐步修复）

- **下划线问题**：如遇到下划线，可能会填写到下划线前或出现其他排版问题（2.1.1 已部分修复）
- **填写内容未成功写入**：某些情况下文档与原始文档一样（2.1.1 已优化验证逻辑）
- **表格填写问题**：表格内容无法正确填写（2.1.1 已新增关键词锚定法）
- **正则表达式匹配问题**：可能无法匹配文档中的实际 XML 结构（2.1.1 已升级为智能填写引擎）

---

### 1.0.0（初始版本）

- 多 Agent 协作系统
- 文件夹分析、代码审查、文档摘要、文件整理
- 跨会话记忆
- 多模型支持

---

## 功能特性

- **智能表单填写**（Ethan）：从文档中提取待填项，对话式收集信息并自动填入文档
- **多 Agent 协作**：Leader Agent 自动分解任务，分配给专业 Agent 并行处理，交叉评审后汇总结果
- **文件夹分析**（Charlotte）：扫描项目结构、技术栈、依赖关系，给出改进建议
- **代码审查**（William）：分析代码质量、安全隐患、性能问题，提供优化方案
- **文档摘要**（Amelia）：提取文档核心内容，生成结构化摘要
- **文件整理**（James）：自动分类、重命名、重组文件夹
- **跨会话记忆**（Sophie）：记住用户偏好和历史交互，提供个性化服务
- **多模型支持**：内置 DeepSeek、OpenAI、Anthropic、Google、智谱、通义千问、Moonshot 等 Provider，兼容任意 OpenAI 兼容 API

## 技术栈

- **前端**：React 18 + TypeScript + Tailwind CSS
- **桌面框架**：Neutralinojs
- **状态管理**：Zustand
- **构建工具**：Vite 5
- **文档处理**：PizZip（docx / xlsx）、pdf-parse（pdf）

## 快速开始

### 环境要求

- Node.js >= 18
- npm >= 9

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev          # 启动 Vite 开发服务器
npm run neu:dev      # 启动 Neutralinojs 桌面应用（带调试器）
```

### 构建桌面应用

```bash
npm run neu:build    # 构建并打包 Neutralinojs 应用
```

### 配置 API

启动应用后，点击底部状态栏的 **配置 API Key**，填入你的 API 地址和密钥即可使用。

## 项目结构

```
src/
  renderer/          # React 渲染进程
    agents/          # Agent 定义与注册
    api/             # Neutralinojs Native API 封装
    codebase/        # 代码库依赖分析
    components/      # UI 组件
    knowledge/       # 知识库检索
    memory/          # 记忆存储
    stores/          # Zustand 状态管理
    styles/          # 样式文件
    utils/           # LLM 调用、文档处理、工具函数
```

## Agent 列表

| Agent | 名称 | 职责 |
|-------|------|------|
| Oliver | Leader Agent | 智能任务调度，理解用户需求并分配给合适的 Agent |
| Charlotte | 文件分析专家 | 分析文件夹结构、文件类型分布、技术栈推断 |
| William | 代码审查专家 | 审查代码质量，发现问题和改进建议 |
| Amelia | 文档摘要专家 | 读取文档内容，总结项目核心信息 |
| James | 文件整理专家 | 根据建议重新分类与整理文件 |
| Sophie | 跨会话记忆专家 | 记住重要信息、回忆历史分析、管理偏好 |
| Ethan | 表单填写专家 | 从文档中提取待填项，对话式收集信息并自动填入文档 |

## License

MIT
